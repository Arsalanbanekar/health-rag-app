# Pinned runtime dependencies
fastapi==0.135.3
uvicorn[standard]==0.44.0
python-dotenv==1.2.1
groq==0.36.0
supabase==2.28.3
pydantic==2.12.4
httpx==0.28.1
python-multipart==0.0.24

# Embeddings — BAAI/bge-small-en-v1.5 (384-dim, 512-token context) via
# fastembed (ONNX Runtime, no torch) — see services/embeddings.py docstring
# for why: ~190MB resident vs ~504MB for the torch-based equivalent.
fastembed==0.8.0

# Ingestion — fetching and parsing source fact sheets (seeding only)
requests==2.33.0
beautifulsoup4==4.15.0
